# Pipeline Configuration

How the entire pipeline is configured (scraper script -> push to GitHub -> push to AGOL (if applicable)), all triggered via a single script in a task in Windows Task Scheduler that is run every x times.
